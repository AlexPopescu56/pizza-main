@page
@model ContosoPizza.Pages.MenuModel
@{
    ViewData["Title"] = "Pizza Menu";
}

<div class="row">
    <div class="col-12">
        <h1>Pizza Menu 🍕</h1>
        <p class="lead">Choose from our delicious selection of pizzas!</p>
    </div>
</div>

<div class="row">
    <div class="col-12">
        @if (Model.PizzaList.Any())
        {
            <div class="table-responsive">
                <table class="table table-striped table-hover">
                    <thead class="table-dark">
                        <tr>
                            <th scope="col">Name</th>
                            <th scope="col">Price</th>
                            <th scope="col">Size</th>
                            <th scope="col">Gluten Free</th>
                            <th scope="col">Actions</th>
                        </tr>
                    </thead>
                    <tbody>
                        @foreach (var pizza in Model.PizzaList)
                        {
                            <tr>
                                <td><strong>@pizza.Name</strong></td>
                                <td class="text-success fw-bold">@($"{pizza.Price:C}")</td>
                                <td>
                                    <span class="badge bg-secondary">@pizza.Size</span>
                                </td>
                                <td>
                                    @if (pizza.IsGlutenFree)
                                    {
                                        <span class="badge bg-success">✔️ Gluten Free</span>
                                    }
                                    else
                                    {
                                        <span class="text-muted">-</span>
                                    }
                                </td>
                                <td>
                                    <form method="post" asp-page-handler="Delete" asp-route-id="@pizza.Id" class="d-inline">
                                        <button class="btn btn-outline-danger btn-sm" type="submit" 
                                                onclick="return confirm('Are you sure you want to delete @pizza.Name?')">
                                            <i class="bi bi-trash"></i> Delete
                                        </button>
                                    </form>
                                </td>
                            </tr>
                        }
                    </tbody>
                </table>
            </div>
        }
        else
        {
            <div class="alert alert-info" role="alert">
                <h4 class="alert-heading">No pizzas available!</h4>
                <p>Looks like our menu is empty. Why not <a asp-page="/AddPizza" class="alert-link">add some pizzas</a> to get started?</p>
            </div>
        }
    </div>
</div>

<div class="row mt-4">
    <div class="col-12">
        <a asp-page="/AddPizza" class="btn btn-primary">
            <i class="bi bi-plus-circle"></i> Add New Pizza
        </a>
        <a asp-page="/Index" class="btn btn-outline-secondary">
            <i class="bi bi-house"></i> Back to Home
        </a>
    </div>
</div>